plugins {
    id 'base'
}

subprojects {
    plugins.withType(JavaBasePlugin).configureEach {
        java {
            toolchain {
                languageVersion = JavaLanguageVersion.of(8)
            }
        }
    }
}
